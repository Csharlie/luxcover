import type { SiteData } from '@spektra/types'

/**
 * Maps raw WordPress REST API response to SiteData.
 * Placeholder — replace with real field mapping when WP backend is ready.
 */
export function mapWordPressSiteData(raw: unknown): SiteData {
  return raw as SiteData
}
